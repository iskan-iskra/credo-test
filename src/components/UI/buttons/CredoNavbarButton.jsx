import React from 'react';
import Cl from './buttons.module.scss'

const CredoNavbarButton = () => {
    return (
        <div className={Cl.CredoNavbarButton}>
            30 дней бесплатно
        </div>
    );
};

export default CredoNavbarButton;